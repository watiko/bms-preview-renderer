{
  "scripts": {
    "dev": "deno run --no-check --allow-all --import-map=./import_map.json ./cli.ts",
    "start": "deno run --allow-all --import-map=./import_map.json ./cli.ts",
    "test": "deno test --unstable --import-map=./import_map.json",
    "repl": "deno repl --import-map=./import_map.json",
    "install": "deno install -f --name bms-preview-renderer --no-check --allow-all --import-map=./import_map.json ./cli.ts",
    "uninstall": "deno uninstall bms-preview-renderer"
  }
}
